use crate::support::System;
use glium::index::IndicesSource;
use glium::texture::Cubemap;
use glium::texture::{Dimensions, MipmapsOption, UncompressedFloatFormat};
use glium::vertex::VerticesSource;
use glium::{BackfaceCullingMode, DrawParameters, Frame, Program, Surface};
use image::io::Reader as ImageReader;
use image::GenericImageView;
use std::any::Any;
use std::fs::File;
use std::io::Read;
use std::path::PathBuf;
use std::sync::Arc;

use super::Material;

#[derive(Clone)]
pub struct SkyboxMat {
    program: Arc<Program>,
    skybox: Arc<Cubemap>,
}

impl SkyboxMat {
    pub fn load_from_fs(system: &System, path: impl Into<PathBuf>) -> Self {
        let facade = &*system.display;
        let mut vertex_shader_file = File::open("shaders/skybox/vertex.glsl").unwrap();
        let mut vertex_shader_src = String::new();
        vertex_shader_file
            .read_to_string(&mut vertex_shader_src)
            .unwrap();
        let mut fragment_shader_file = File::open("shaders/skybox/fragment.glsl").unwrap();
        let mut fragment_shader_src = String::new();
        fragment_shader_file
            .read_to_string(&mut fragment_shader_src)
            .unwrap();

        let program =
            Program::from_source(facade, &vertex_shader_src, &fragment_shader_src, None).unwrap();

        println!("Loading cubemap");
        let cubemap = load_cubemap(path.into(), system);
        println!("Finished loading cubemap");

        Self {
            program: Arc::new(program),
            skybox: Arc::new(cubemap),
        }
    }
}
impl Material for SkyboxMat {
    fn render<'a>(
        &self,
        vertex_buffer: VerticesSource<'a>,
        index_buffer: IndicesSource<'a>,
        surface: &mut Frame,
        camera: [[f32; 4]; 4],
        position: [[f32; 4]; 4],
    ) {
        let camera_pos: [f32; 3] = [position[3][0], position[3][1], position[3][2]];

        let cubemap = &self.skybox;

        let uniforms = uniform! {
            projection: camera,
            view: position,
            camera_pos: camera_pos,
            skybox: &**cubemap,
        };

        surface
            .draw(
                vertex_buffer,
                index_buffer,
                &*self.program,
                &uniforms,
                &DrawParameters {
                    backface_culling: BackfaceCullingMode::CullCounterClockwise,
                    depth: glium::Depth {
                        test: glium::DepthTest::IfLessOrEqual,
                        write: true,
                        ..Default::default()
                    },
                    ..Default::default()
                },
            )
            .unwrap();
    }

    fn equal(&self, material: &dyn Any) -> bool {
        let _simple = match material.downcast_ref::<Self>() {
            Some(simple) => simple,
            None => return false,
        };

        true
    }

    fn to_any(self) -> Box<dyn Any> {
        Box::new(self)
    }
    fn as_any(&self) -> &dyn Any {
        self
    }
    fn as_any_mut(&mut self) -> &mut dyn Any {
        self
    }
    fn clone_material(&self) -> Box<dyn Material> {
        Box::new(self.clone())
    }
    fn clone_sized(&self) -> Self
    where
        Self: Sized,
    {
        self.clone()
    }
}
fn load_cubemap(path: PathBuf, system: &System) -> Cubemap {
    let path: PathBuf = path.into();

    gl::load_with(|s| system.display.gl_window().get_proc_address(s));
    let cubemap_id = load_cubemap_gl(vec![
        path.with_file_name("right.jpg"),
        path.with_file_name("left.jpg"),
        path.with_file_name("top.jpg"),
        path.with_file_name("bottom.jpg"),
        path.with_file_name("front.jpg"),
        path.with_file_name("back.jpg"),
    ]);

    let facade = &*system.display;
    //let cubemap = Cubemap::empty(facade, 6).unwrap();
    unsafe {
        let cubemap = Cubemap::from_id(
            facade,
            UncompressedFloatFormat::U8U8U8,
            cubemap_id,
            true,
            MipmapsOption::AutoGeneratedMipmaps,
            //Dimensions::Texture2d { width, height },
            Dimensions::Cubemap { dimension: 6 },
        );

        return cubemap;
    }
}
fn load_cubemap_gl(faces: Vec<PathBuf>) -> u32 {
    unsafe {
        let mut texture_id: u32 = 0;

        gl::GenTextures(1, &mut texture_id);
        gl::BindTexture(gl::TEXTURE_CUBE_MAP, texture_id);

        for i in 0..faces.len() {
            let texture = ImageReader::open(&faces[i]).unwrap().decode().unwrap();

            let (width, height) = texture.dimensions();
            let mut pixels = texture.to_rgb8();

            let flat_samples = pixels.as_flat_samples_mut();
            let slice: &[u8] = flat_samples.as_slice();
            let ptr: *const core::ffi::c_void = slice as *const _ as *const core::ffi::c_void;

            gl::TexImage2D(
                gl::TEXTURE_CUBE_MAP_POSITIVE_X + i as u32,
                0,
                gl::RGB as i32,
                width as i32,
                height as i32,
                0,
                gl::RGB,
                gl::UNSIGNED_BYTE,
                ptr,
            );
        }

        gl::TexParameteri(
            gl::TEXTURE_CUBE_MAP,
            gl::TEXTURE_MIN_FILTER,
            gl::LINEAR as i32,
        );
        gl::TexParameteri(
            gl::TEXTURE_CUBE_MAP,
            gl::TEXTURE_MAG_FILTER,
            gl::LINEAR as i32,
        );
        gl::TexParameteri(
            gl::TEXTURE_CUBE_MAP,
            gl::TEXTURE_WRAP_S,
            gl::CLAMP_TO_EDGE as i32,
        );
        gl::TexParameteri(
            gl::TEXTURE_CUBE_MAP,
            gl::TEXTURE_WRAP_T,
            gl::CLAMP_TO_EDGE as i32,
        );
        gl::TexParameteri(
            gl::TEXTURE_CUBE_MAP,
            gl::TEXTURE_WRAP_R,
            gl::CLAMP_TO_EDGE as i32,
        );

        return texture_id;
    }
}
